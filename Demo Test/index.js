let person = {
    name : "Paresh",
    age : 20,
    country : "India"
}

function logData(){
    //Paresh is 20 years old and lives in india
    console.log(person.name +" is "+ person.age + " years old and lives in " + person.country);
}
logData();
